config MODEL_IDENTYFICATION_DRV
    bool "motor controller module"
    depends on LOG_OVER_SERIAL

if MODEL_IDENTYFICATION_DRV

config MODEL_IDENTYFICATION_LOG
    bool "Enable logging from model identyfication driver"
    depends on MODEL_IDENTYFICATION_DRV

endif #REGULATOR_DRV